#include "DatabaseNoradScheduleSaver.h"

bool DatabaseNoradScheduleSaver::save(const std::vector<NORAD_SCHEDULE>& vecNoradSchedule) const {;
    Q_UNUSED(vecNoradSchedule);
    // logic for DB
    return true;
}
